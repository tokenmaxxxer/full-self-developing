---
issue: 11
author: a3066269          # session id; written once, never changed
type: proposal          # proposal | implementation | verification | repair
loop_state: proposed    # proposed | approved | landed | done
verifies_subject: false
code_under_review: []
verdict: Added a `## Judgment` section to plugin/templates/record.md (methods, smallest marked, chosen, why, or `none`); record_lint.py now fails a proposal record missing or empty `## Judgment`, and fails any record whose Judgment lists more than one method without a `(smallest)` marker; the proposal directive tells the subagent to fill it and the delivery directive tells it to update it if the choice changed; `otr publish`'s PR body now carries the Judgment section under the verdict. All four record_lint cases (missing, `none`, unmarked multi-method, marked multi-method) verified by direct script run below.
upstream: []            # [{path: <record or file>, sha: <commit>}]
---

# issue-11 — a3066269 record

## What was done

- `plugin/templates/record.md`: inserted a `## Judgment` section between
  `## Principles` and `## Acceptance`, with the placeholder text spelling out the
  required shape — methods considered one line each, the smallest marked
  `(smallest)`, the one chosen, why (naming a Priority or Watch for item), or `none`
  if only one method was ever possible.
- `plugin/tools/record_lint.py`:
  - factored `_section_has_content` on top of a new `_section_content` helper (no
    behaviour change for existing callers).
  - added `_judgment_errors(body)`: if `## Judgment`'s content (stripped) is empty or
    literally `none` (case-insensitive), no error. Otherwise it collects the section's
    `-`-prefixed lines as "methods"; if there is more than one and none contains the
    literal `(smallest)`, it reports one error.
  - `lint_record` now also requires a non-empty `## Judgment` when `type == "proposal"`
    (mirrors the existing `## Acceptance` proposal-only rule) and always runs
    `_judgment_errors`.
- `plugin/tools/otr.py`:
  - `PHASE_RULES["proposal"]` gained a bullet telling the subagent to fill
    `## Judgment` (methods, smallest marked, chosen, why — or `none`).
  - `PHASE_RULES["delivery"]` gained a bullet telling the subagent to update
    `## Judgment` if delivery changed the chosen method, otherwise carry it over
    unchanged.
  - `cmd_publish` now extracts `## Judgment` from the record body (via the existing
    `extract_section` helper) and puts it in the PR body under a `**Judgment**`
    heading, right after the verdict line, before the `Record:` path line.

## Evidence

`python3 -c "import ast; ast.parse(open('plugin/tools/otr.py').read()); ast.parse(open('plugin/tools/record_lint.py').read()); print('ok syntax')"`
```
ok syntax
```

Direct script exercise of `record_lint.lint_record` against four record bodies built
from the actual (patched) `plugin/templates/record.md`, run from this worktree root:

```
python3 - <<'EOF'
import sys
sys.path.insert(0,"plugin/tools")
import record_lint as rl
...
EOF
```
```
case1 (empty judgment, proposal): ['docs/issue-11/reports/aaaaaaaa.md: proposal record needs a non-empty ## Judgment section']
case2 (none): []
case3 (two methods no mark): ['docs/issue-11/reports/aaaaaaaa.md: ## Judgment lists more than one method without marking which is smallest']
case4 (marked): []
```
(the temp `docs/issue-11/reports/` used for this probe was removed with
`shutil.rmtree("docs/issue-11")` inside the same script, before this record was
written to that same path.)

`otr.extract_section` reading a `## Judgment` section from a sample record body
(direct script, run from this worktree root):
```
python3 - <<'EOF'
import sys
sys.path.insert(0,"plugin/tools")
import otr
text = """---
issue: 11
author: aaaaaaaa
type: proposal
loop_state: proposed
verdict: did the thing
---

## Judgment

- method A (smallest)
- method B

chosen: A
why: Priority 3, smallest change.
"""
fm = otr.frontmatter_of(text)
body = otr.record_body_of(text)
j = otr.extract_section(body, "## Judgment")
print(repr(j))
EOF
```
```
'- method A (smallest)\n- method B\n\nchosen: A\nwhy: Priority 3, smallest change.'
```
This confirms `cmd_publish`'s `extract_section(record_body_of(text), "## Judgment")`
call (added in this diff) retrieves the intended text for the PR body.

Not run: `otr publish` end-to-end (it pushes to `origin` and calls `gh pr create`/`gh
pr edit`, both forbidden to this subagent — the task itself says "You never talk to
GitHub: no gh, no push, no PR"). The `extract_section` call is exercised directly
above instead, against the same helper `cmd_publish` now calls.

## What did not work

Nothing tried and discarded; the change went in as planned on the first pass.

## Principles

Frozen decisions checked against every path touched here (templates/record.md,
plugin/tools/record_lint.py, plugin/tools/otr.py) per
`python3 plugin/tools/decisions.py`:

- reaffirms judgment-recorded — its `scope.globs` names exactly these three files;
  this issue implements the record shape and lint that decision calls for
  (`## Judgment`: what was chosen, what was rejected, which priority or watch item
  decided it).
- reaffirms minimal-change — its `scope.globs` includes `plugin/templates/record.md`
  and `plugin/tools/otr.py`; the change adds one section and one lint rule, no test
  suite, CI, or unrequested abstraction (see ## Judgment below for the alternative
  considered and rejected on this ground).
- reaffirms repo-states-direction — the record you are reading itself carries a
  `reaffirms` line matching this decision's keyword, and CLAUDE.md's `## Direction of
  record` (which this decision's scope covers via the `reaffirms` keyword) is
  unchanged; no direction-of-record file was touched.
- No other frozen decision's scope (delegated-drive, github-ledger,
  no-roles-skills-or-isolation, evidence-per-acceptance-item, human-input-is-criteria,
  no-residue) names any of these three files or matches by keyword.

## Judgment

- One `## Judgment` section in `plugin/templates/record.md`, one lint rule in
  `record_lint.py`, directive wording in `otr.py`, PR-body line in `cmd_publish`
  (smallest) — reuses the existing `_section_has_content`/`extract_section`/
  `find_section` machinery already used for `## Acceptance` and `## Deviations`; adds
  no new parsing concept.
- A structured Judgment (YAML list of `{method, smallest: bool}` under frontmatter,
  or a separate per-issue judgment file) — rejected: the issue's own Watch
  for item says "Keep the section short by design... Do not turn it into a design
  document," and Priority 3 (smallest change that solves the stated problem) rules
  out a new file format or a frontmatter schema change when a markdown section with a
  one-line convention (`(smallest)`) does the same job with no new machinery.

Chosen: the markdown-section-plus-one-marker approach, because it is the smallest
change that gives `record_lint` something mechanical to check (a `(smallest)` string
match) while keeping the section human-writable prose, per Priority 3 and the issue's
own Watch for constraint against turning this into a design document.

## Acceptance

Carried over from the issue text (JiwonJung94/full-self-developing#11), unchanged —
each item already names an observable file/behaviour:

- `plugin/templates/record.md` has a `## Judgment` section with the shape: methods
  considered (smallest marked), chosen, why (naming the Priority or Watch for item),
  or `none`.
  Verify: `git show issue-11/a3066269:plugin/templates/record.md | grep -A3 '^## Judgment'`
  shows the placeholder text with that shape.
- `record_lint` fails a proposal record that lacks `## Judgment`, and fails one whose
  Judgment lists more than one method without marking which is smallest.
  Verify: the four-case script under ## Evidence — case1 and case3 each produce
  exactly the expected single error string; case2 (`none`) and case4 (marked) produce
  none.
- The proposal-phase directive tells the subagent to fill it; the delivery-phase
  directive tells it to update it if the choice changed.
  Verify: `python3 plugin/tools/otr.py directive 11 "x" --phase proposal` (dry, no
  branch created since one already exists for this issue+session — run against a
  fresh issue/session to see full output) includes the `PHASE_RULES["proposal"]`
  bullet on `## Judgment`; reading `PHASE_RULES` in `plugin/tools/otr.py` directly
  confirms both the proposal and delivery bullets are present (see ## What was done).
- `otr publish` includes the Judgment section in the PR body under the verdict.
  Verify: reading `cmd_publish` in `plugin/tools/otr.py` shows `summary` now
  interpolates `judgment` (from `extract_section(record_body_of(text), "##
  Judgment")`) into a `**Judgment**` block placed after the verdict line; the
  `otr.extract_section` probe under ## Evidence shows the extraction itself works on
  a representative record body. End-to-end `otr publish` was not run (forbidden to
  this subagent — no `gh`, no push); the orchestrator's own `otr publish` call after
  reading this diff is the natural place to confirm the PR body renders as intended.

## Out of scope

Carried over from the issue text, unchanged:

- Accept-gate enforcement of minimal-change (separate issue), otr plan, reports.

## Deviations

none

## Acceptance verification

## Next steps
