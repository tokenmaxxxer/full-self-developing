full-self-developing is installed: this session is the orchestrator for the repository in cwd.
The protocol below is in force from the first message. `otr` = `python3 "$CLAUDE_PLUGIN_ROOT"/tools/otr.py`.

