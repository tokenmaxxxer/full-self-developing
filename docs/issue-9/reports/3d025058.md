---
issue: 9
author: 3d025058          # session id; written once, never changed
type: proposal          # proposal | implementation | verification | repair
loop_state: proposed    # proposed | approved | landed | done
verifies_subject: false
code_under_review: []
verdict: Three small text edits satisfy every Acceptance item — otr init's north pole
  template (plugin/tools/otr.py:144), the directive's north pole paragraph
  (plugin/tools/otr.py:266 DIRECTIVE string), and the "Direction of record" + "GAP"
  sentences in CLAUDE.md and plugin/commands/run.md — no code paths, options, or other
  templates touched; `grep -rn "GAP\|served by\|numbered heading" plugin/tools/otr.py
  CLAUDE.md plugin/commands/run.md` after the edit finds none of the old markers.
upstream: []            # [{path: <record or file>, sha: <commit>}]
---

# issue-9 — 3d025058 record

## What was done

Proposal only (phase: proposal) — no implementation, per PHASE rules. The changes below
are the smallest edit that makes each Acceptance item true; they replace old-shape text
in place, they don't add files, options, or commands.

### 1. `plugin/tools/otr.py` — `cmd_init`'s north pole template (line 144)

Current (grep'd, see Evidence):
```
put("docs/specs/northpole.md", f"# North pole\n\nWhat this repository is for, as
currently understood. Edited in place as\nthinking changes; history is `git log -p` on
this file.\n\n## N0 — \n\n- since {today} · revised {today}\n- served by: GAP\n")
```

Proposed replacement (same `put(...)` call, new template string):
```python
put("docs/specs/northpole.md", (
    "# North pole\n\n"
    "What the human has decided for this repository. Five sections, each holding\n"
    "only the human's own words. Edited in place as thinking changes; history is\n"
    "`git log -p` on this file.\n\n"
    "## Problem\n\n"
    "(which problem, and why — the human's own words)\n\n"
    f"- since {today} · revised {today}\n\n"
    "## Watch for\n\n"
    "(what could go wrong or be overdone as work proceeds)\n\n"
    f"- since {today} · revised {today}\n\n"
    "## Constraints\n\n"
    "(what may not be crossed)\n\n"
    f"- since {today} · revised {today}\n\n"
    "## Priorities\n\n"
    "(when two goods conflict, which wins — ranked)\n\n"
    f"- since {today} · revised {today}\n\n"
    "## Must first\n\n"
    "(what must happen before anything else is worth accepting)\n\n"
    f"- since {today} · revised {today}\n"
))
```
This is the exact five headings from `docs/specs/northpole.md` and
`docs/decisions/2026-09-09-repo-states-direction.md`, one placeholder instruction line
per section (mirroring how the real file reads), and the `since`/`revised` pair the real
file already carries per section — no `served by`, no `GAP`, no numbering.

Alternative considered: a single flat instruction ("edit the five sections below") with
no per-section `since`/`revised` scaffolding. Rejected — the live `docs/specs/northpole.md`
carries `since`/`revised` per section (Priorities, `judgment-recorded`); a freshly
initialised repo should start in the same shape its own decisions already require, not a
shape the human has to fix on first edit.

### 2. `plugin/tools/otr.py` — `DIRECTIVE`'s north pole paragraph (in the `DIRECTIVE`
   string, currently the "BEFORE ANYTHING ELSE" paragraph)

Current:
```
BEFORE ANYTHING ELSE read docs/specs/northpole.md (what this repo is for) and every
`status: frozen` file in docs/decisions/ (`python3 {tools}/decisions.py` lists them). A change
that works against either is a deviation to record, never a judgment call to make.
```

Proposed replacement:
```
BEFORE ANYTHING ELSE read docs/specs/northpole.md — Problem, Watch for, Constraints,
Priorities, Must first — and every `status: frozen` file in docs/decisions/ (`python3
{tools}/decisions.py` lists them). Choose methods by Priorities, turn Watch for into
checks, order work by Must first, and record such judgment under ## Judgment; a change
against Constraints or a frozen decision is a Deviation to record, never a judgment call
to make.
```
This names the five sections and states, in the directive every subagent reads every
session: methods are chosen by Priorities, Watch for becomes checks, Must first orders
work, such judgment is recorded under `## Judgment` (matching
`docs/decisions/2026-09-09-judgment-recorded.md`, which already names that heading), and
a Constraints violation is a Deviation — the exact four clauses the Acceptance item lists.
It stays one paragraph, same place in the directive, ~8 words longer than the original
(62 → ~70 words) — kept short per the issue's Watch for.

Alternative considered: leaving the paragraph as "read X and Y, a change against either is
a deviation" and adding the Priorities/Watch-for/Must-first instructions as a *second*
paragraph. Rejected as not smaller: it would add a whole paragraph instead of extending
one sentence, working against the issue's own "keep the directive short" Watch for.

### 3. `CLAUDE.md` and `plugin/commands/run.md` — "Direction of record" section

Both files carry an identical copy of this bullet (CLAUDE.md:41-47, run.md:45-51);
current text:
```
- `docs/specs/northpole.md` states what the repo is for *as currently understood*. It is
  managed, not appended: when the human states, changes, or sharpens a direction, edit
  the matching entry in place (bump `revised`), merge entries that now say one thing, or
  remove one that no longer holds — and say in the commit message what changed and why.
  Add a new `## N&lt;k&gt;` only for a genuinely new intent. Read the current file before
  editing; never reconstruct it from memory of the conversation.
```
Proposed replacement (both files, same text):
```
- `docs/specs/northpole.md` holds the human's Problem, Watch for, Constraints, Priorities
  and Must first — five sections, each only the human's own words. It is managed, not
  appended: when the human states, changes, or sharpens one, edit that section in place
  (bump its `revised`), merge lines that now say one thing, or remove one that no longer
  holds — and say in the commit message what changed and why. Read the current file
  before editing; never reconstruct it from memory of the conversation.
```
Drops the numbered-entry (`## N&lt;k&gt;`) language the five fixed sections have replaced; adds
nothing new (still: edit in place, bump `revised`, say why in the commit).

### 4. `CLAUDE.md` and `plugin/commands/run.md` — the "GAP" sentence in the Drive section

Both files carry an identical sentence (CLAUDE.md:75-78, run.md:79-82); current:
```
The destination is only what the human said. Under delegation the orchestrator may open
an issue for exactly two reasons: the human stated the need in this conversation, or a
subagent's `## Deviations` names it (`--origin` pointing at that session). The north pole
is a constraint on how work is done, never a source of work: a `GAP` there, an improvement
you notice, a "while we're here" — none of these become issues. Put them in the final
report's **limits**; the human decides. An orchestrator that starts filling gaps on its
own has left the road.
```
Proposed replacement of only the fourth sentence (rest unchanged):
```
The north pole
is a constraint on how work is done, never a source of work: an unserved need you notice
there, an improvement, a "while we're here" — none of these become issues. Put them in the
final report's **limits**; the human decides. An orchestrator that starts filling gaps on
its own has left the road.
```
`GAP` was the old template's literal marker (`served by: GAP`) for an unserved N-entry;
now that no section carries that marker, the sentence is reworded to the same meaning
("something the north pole doesn't yet cover") without naming the retired marker.

Alternative considered: leaving this sentence untouched, since the Acceptance item names
CLAUDE.md's "Direction of record" specifically. Rejected — the issue's Problem and its
own last Acceptance bullet ("no longer mention `served by` or GAP entries") are not
scoped to one section; this is the other and only other `GAP` occurrence in either file,
and leaving it produces the same drift the issue is about (a stale reference to a
template shape that no longer exists) two paragraphs later in the same document.

## Evidence

Current template text (before this proposal), showing every place the old shape appears:
```
$ grep -n "GAP\|served by\|N0\|N1\|N2\|N3\|N4\|N5" plugin/commands/run.md CLAUDE.md docs/specs/northpole.md plugin/tools/otr.py plugin/templates/*.md
plugin/commands/run.md:80:is a constraint on how work is done, never a source of work: a `GAP` there, an improvement
CLAUDE.md:76:is a constraint on how work is done, never a source of work: a `GAP` there, an improvement
plugin/tools/otr.py:144:    put("docs/specs/northpole.md", f"# North pole\n\nWhat this repository is for, as currently understood. Edited in place as\nthinking changes; history is `git log -p` on this file.\n\n## N0 — \n\n- since {today} · revised {today}\n- served by: GAP\n")
docs/specs/northpole.md:25:- since 2026-09-08 · revised 2026-09-09 (was "full self-developing" + N5; now states the human's job as criteria, not design)
docs/specs/northpole.md:60:- since 2026-09-08 · revised 2026-09-09 (N1–N4 folded here and into decisions; single-session drive accepted)
plugin/commands/run.md:50:  Add a new `## N&lt;k&gt;` only for a genuinely new intent. Read the current file before
CLAUDE.md:46:  Add a new `## N&lt;k&gt;` only for a genuinely new intent. Read the current file before
```
(the two hits inside `docs/specs/northpole.md` itself are its own commit-history notes,
not template shape — out of scope, untouched.)

No tests exist to run unchanged (issue's Acceptance: "existing tests (if any) pass with
the same command as before"):
```
$ find . -iname "*test*" -not -path "./runs/*" -not -path "./.git/*" | grep -v docs/issue
(no output)
```

The live `docs/specs/northpole.md` already carries the five headings and the
`since`/`revised` shape the proposed `otr init` template copies:
```
$ grep -n "^## \|since .* · revised" docs/specs/northpole.md
## Problem
- since 2026-09-08 · revised 2026-09-09 (was "full self-developing" + N5; now states the human's job as criteria, not design)
## Watch for
- since 2026-09-09 · revised 2026-09-09
## Constraints
- since 2026-09-08 · revised 2026-09-09 (N1–N4 folded here and into decisions; single-session drive accepted)
## Priorities
- since 2026-09-09 · revised 2026-09-09
## Must first
- since 2026-09-09 · revised 2026-09-09
```

`## Judgment` is already the heading `judgment-recorded` names, confirming the directive
wording in change 2 points at a real, existing target:
```
$ sed -n '1,30p' docs/decisions/2026-09-09-judgment-recorded.md | grep -n "Judgment"
15:When an agent chooses between methods by the north pole's Priorities, or
21:goes in the record under `## Judgment`: what was chosen, what was rejected, which
```

## What did not work

Nothing tried and abandoned — this is a text-only proposal, verified by reading the
current source and the frozen decisions it must not work against.

## Principles

- reaffirms human-input-is-criteria — scope includes `docs/specs/northpole.md`,
  `plugin/tools/otr.py`, `CLAUDE.md`, `plugin/commands/run.md`; this proposal keeps the
  human's five inputs (Problem, Watch for, Constraints, Priorities, Must first) as the
  only things the template asks for and adds no Acceptance/Out-of-scope fields to it.
- reaffirms repo-states-direction — scope includes `docs/specs/northpole.md`; the
  proposed template states the same five-section shape this decision defines, and adds
  no numbered entries or log-like history.
- reaffirms judgment-recorded — scope includes `plugin/tools/otr.py`, `CLAUDE.md`; the
  proposed directive wording is the one place that tells every subagent to record
  Priorities/Watch-for judgment under `## Judgment`, which is this decision's own
  requirement.
- reaffirms minimal-change — scope includes `plugin/tools/otr.py`; each of the four edits
  above replaces old text in place with the smallest wording that satisfies one
  Acceptance item — no new files, options, commands, tests, or CI.

## Deviations

None. Everything needed to satisfy issue #9's Acceptance is inside the four edits above,
all within the files the issue's own scope names (`otr init`'s template, the directive
text, CLAUDE.md and run.md's north pole references).

## Acceptance verification

Not applicable at this phase (loop_state: proposed) — this is a proposal record; per-item
acceptance verification with commands is required at delivery (loop_state: landed) once
the four edits above are actually applied to `plugin/tools/otr.py`, `CLAUDE.md`, and
`plugin/commands/run.md`.

## Next steps

On approval, delivery applies the four edits verbatim as drafted above, then verifies:
`otr init` in a scratch empty repo writes the five headings with no `served by`/`GAP`;
`otr directive` output contains the five section names and the four clauses; `grep -rn
"GAP\\|served by\\|## N&lt;k&gt;"` over `plugin/tools/otr.py CLAUDE.md plugin/commands/run.md`
finds nothing.
