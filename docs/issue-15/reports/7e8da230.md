---
issue: 15
author: 7e8da230
type: implementation
loop_state: landed
verifies_subject: false
code_under_review: []
verdict: Step 1 in CLAUDE.md and plugin/commands/run.md drafts `## Problem` and `## Watch for` from the conversation, reads it back once, then runs `otr issue`, noting Acceptance and Out of scope arrive with the approved proposal — the exact wording the issue's Acceptance asks for. `grep -n "## Need" CLAUDE.md plugin/commands/run.md` finds no remaining `## Need` mention in either file. `diff` of the section across both files is empty (identical wording). No otr.py or template file touched. Landed on the proposal commit already; this delivery pass merged main and rewrote the record as implementation/landed with corrected Principles.
upstream: []
---

# issue-15 — 7e8da230 record

## What was done

Replaced step 1 of "When the human states a need" in both `CLAUDE.md` and
`plugin/commands/run.md` (the two files carry this section verbatim) with:

```
## When the human states a need
1. Draft the issue from the conversation — `## Problem` and `## Watch for` — read it back
   once, then run `otr issue` with the title and body. The GitHub issue is the
   requirement of record; Acceptance and Out of scope arrive with the approved proposal.
```

This replaces the prior "`## Need`, `## Acceptance` (observable conditions), `## Out of
scope`" wording, matching decision `human-input-is-criteria` (the human writes
`## Problem` / `## Watch for`; Acceptance and Out of scope come from the approved
proposal, per issue #10). No other line in either file was touched; no otr.py, no
template. This was done in the proposal-phase commit and carries through delivery
unchanged — the approved `## Acceptance` on the issue matched exactly what was already
implemented, so no further code change was needed for delivery. This pass merged
`main` (picking up issue #10's landed changes to `otr.py`, `record_lint.py`, and
`record.md`) and rewrote this record as `type: implementation`, `loop_state: landed`,
with `## Acceptance verification` and corrected `## Principles`.

## Evidence

- `git merge --no-edit main` → `Merge made by the 'ort' strategy.` (4 files changed:
  `docs/issue-10/reports/0e3efb62.md`, `plugin/templates/record.md`,
  `plugin/tools/otr.py`, `plugin/tools/record_lint.py`; no conflicts, `CLAUDE.md` and
  `plugin/commands/run.md` untouched by the merge).
- `grep -n "## Need" CLAUDE.md plugin/commands/run.md` → no output, exit code 1 (no
  remaining mention of `## Need` as the issue shape in either file).
- `diff <(sed -n '/^## When the human states a need/,/^## Delegating/p' CLAUDE.md) <(sed -n '/^## When the human states a need/,/^## Delegating/p' plugin/commands/run.md)`
  → no output (both files carry the same new step-1 text).
- `git diff main -- CLAUDE.md plugin/commands/run.md` (against the pre-change `main` tip
  at the time of the proposal commit) shows only the step-1 paragraph changed in each
  file, one line shorter — step 1 stays as short as before.
- `python3 plugin/tools/record_lint.py docs/issue-15/reports/7e8da230.md` → `ok: 1
  record(s)`.

## What did not work

Nothing; this was a direct text substitution, no probes needed.

## Principles

- `reaffirms human-input-is-criteria` — makes CLAUDE.md and run.md say what that
  decision requires (human writes Problem/Watch for; Acceptance/Out of scope come from
  the approved proposal); its scope globs list both files by name.
- `reaffirms repo-states-direction` — this record uses the keyword "reaffirms",
  matching that decision's scope keywords.
- `reaffirms delegated-drive` — its scope globs list `CLAUDE.md` and
  `plugin/commands/run.md`; this change only reworded step 1's issue-drafting
  instruction and did not touch the delegated-drive loop (approve/accept/deviation
  steps), so the decision's mechanism is unaffected and still holds.
- `reaffirms github-ledger` — its scope globs list the same two files; the change
  leaves "the GitHub issue is the requirement of record" and the publish/approve/accept
  flow exactly as they were, only rewording how the issue body is drafted.
- `reaffirms no-roles-skills-or-isolation` — its scope globs list `CLAUDE.md`; step 1's
  reword touches no role, skill, sandbox, or isolation text, so this decision is
  unaffected and still holds.
- `reaffirms judgment-recorded` — its scope globs list `plugin/templates/record.md`,
  `plugin/tools/record_lint.py`, `plugin/tools/otr.py`, and `CLAUDE.md`; this record
  is written in `CLAUDE.md`'s scope and includes this `## Principles`/judgment section
  itself, so the decision's requirement (judgment recorded, not only results) is met
  by this record's `## Judgment` section below.
- Other frozen decisions (evidence-per-acceptance-item, minimal-change, no-residue) do
  not name `CLAUDE.md`, `plugin/commands/run.md`, or this record's content in their
  scope globs/keywords, and this change is text-only, so they are not touched.

## Acceptance

Carried over unchanged from the approved proposal (issue #15's current `## Acceptance`
matches what the proposal already implemented):
- CLAUDE.md step 1 and run.md say: draft `## Problem` and `## Watch for` from the
  conversation, read back once, then `otr issue`; Acceptance and Out of scope arrive
  with the approved proposal.
- No remaining mention of `## Need` as the issue shape in either file.

## Out of scope

Carried over unchanged from the issue: any otr.py or template change.

## Deviations

none.

## Acceptance verification

- "CLAUDE.md step 1 and run.md say: draft `## Problem` and `## Watch for` from the
  conversation, read back once, then `otr issue`; Acceptance and Out of scope arrive
  with the approved proposal." — verified by reading step 1 in both files (quoted in
  `## What was done` above) and by
  `diff <(sed -n '/^## When the human states a need/,/^## Delegating/p' CLAUDE.md) <(sed -n '/^## When the human states a need/,/^## Delegating/p' plugin/commands/run.md)`
  returning no output.
- "No remaining mention of `## Need` as the issue shape in either file." — verified by
  `grep -n "## Need" CLAUDE.md plugin/commands/run.md` returning no matches (exit code
  1).

## Judgment

Priorities #3 (smallest change) decided the approach: only step 1's issue-drafting
sentence was reworded in each file; nothing else in either file — including the rest of
the delegated-drive loop text that also mentions issues — was touched, since the issue's
Watch for asked to "keep step 1 as short as it is now" and change nothing else. No
method was rejected; the fix was a direct text substitution matching the issue's
Acceptance wording verbatim, so there was no alternative to weigh.

## Next steps

None; delivery complete, awaiting orchestrator publish/accept.
