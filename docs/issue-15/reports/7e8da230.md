---
issue: 15
author: 7e8da230
type: proposal
loop_state: proposed
verifies_subject: false
code_under_review: []
verdict: Reworded step 1 in CLAUDE.md and plugin/commands/run.md to draft `## Problem` and `## Watch for` from the conversation, read back once, then `otr issue`, with Acceptance and Out of scope noted as arriving with the approved proposal — matching the issue's Acceptance wording. `grep -n "## Need" CLAUDE.md plugin/commands/run.md` finds no remaining `## Need` mention. Both files carry the same replacement text. No otr.py or template file touched.
upstream: []
---

# issue-15 — 7e8da230 record

## What was done

Replaced step 1 of "When the human states a need" in both `CLAUDE.md` and
`plugin/commands/run.md` (the two files carry this section verbatim) with:

```
## When the human states a need
1. Draft the issue from the conversation — `## Problem` and `## Watch for` — read it back
   once, then run `otr issue` with the title and body. The GitHub issue is the
   requirement of record; Acceptance and Out of scope arrive with the approved proposal.
```

This replaces the prior "`## Need`, `## Acceptance` (observable conditions), `## Out of
scope`" wording, matching decision `human-input-is-criteria` (the human writes
`## Problem` / `## Watch for`; Acceptance and Out of scope come from the approved
proposal, per issue #10). No other line in either file was touched; no otr.py, no
template.

## Evidence

- `grep -n "## Need" CLAUDE.md plugin/commands/run.md` → no output, exit code 1 (no
  remaining mention of `## Need` as the issue shape in either file — satisfies the
  Acceptance's second bullet).
- `diff <(sed -n '/^## When the human states a need/,/^## Delegating/p' CLAUDE.md) <(sed -n '/^## When the human states a need/,/^## Delegating/p' plugin/commands/run.md)`
  → no output (both files carry the same new step-1 text — satisfies the issue's Watch
  for "Both files carry the same text; change both identically").
- `git diff main -- CLAUDE.md plugin/commands/run.md` shows only the step-1 paragraph
  changed in each file (four lines removed, three added, net one line shorter) —
  satisfies the Watch for "keep step 1 as short as it is now."

## Acceptance

- CLAUDE.md step 1 and run.md say: draft `## Problem` and `## Watch for` from the
  conversation, read back once, then `otr issue`; Acceptance and Out of scope arrive
  with the approved proposal. Verified by reading both files' step 1 (quoted above) and
  by `diff` of the section across the two files (below).
- No remaining mention of `## Need` as the issue shape in either file. Verified by
  `grep -n "## Need" CLAUDE.md plugin/commands/run.md` returning no matches.

## What did not work

Nothing; this was a direct text substitution, no probes needed.

## Principles

- `reaffirms human-input-is-criteria` — this change makes CLAUDE.md and run.md say what
  that decision requires (human writes Problem/Watch for; Acceptance/Out of scope come
  from the approved proposal); its scope globs list both files by name.
- `reaffirms repo-states-direction` — this record itself uses the keyword "reaffirms",
  matching that decision's scope keywords.
- none of the other 8 frozen decisions (delegated-drive, github-ledger,
  no-roles-skills-or-isolation, evidence-per-acceptance-item, judgment-recorded,
  minimal-change, no-residue) name `CLAUDE.md`, `plugin/commands/run.md`, or this
  record's content in their scope globs/keywords.

## Deviations

none.

## Acceptance verification

## Next steps

Awaiting publish/approval; no delivery-phase actions taken (proposal phase only).
