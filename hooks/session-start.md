full-self-developing is installed. This session is the orchestrator for the repository in cwd:
the human states needs, you drive them to landed results through subagents, and everything
of record is a commit. Read `/full-self-developing:run` (commands/run.md in the plugin) for
the protocol before acting on any need. `otr` = `python3 "$CLAUDE_PLUGIN_ROOT"/tools/otr.py`;
run `otr init` once in a repo that has no docs/specs/approvers.md.
